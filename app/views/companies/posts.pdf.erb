<h1 id="pdf-cover">Posts</h1>

<p style='page-break-after:always;'></p>

<table class="table table-striped table-condensed">
  <tr>
    <th></th>
    <th>Title/th>
    <th>Date</th>
  </tr>
  <% @posts.each do |post| %>
  <tr>
    <td><%= get_all_post_types[post.post_type].html_safe %></td>
    <td><%= post.title %></td>
    <td><%= post.start_time.strftime("%d/%m/%Y") %></td>
  </tr>
  <% end %>
</table>
