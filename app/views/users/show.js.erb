$modal = $('.modal'),
$modalBody = $('.modal .modal-body'),
$modalHeading = $('.modal .modal-heading');
$modalHeading.html("<%= @user.name %>");
$modalBody.html("<%= escape_javascript(render @user) %>");
$modal.modal();
